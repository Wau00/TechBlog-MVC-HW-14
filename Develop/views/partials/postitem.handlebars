<div class="card text-white bg-dark mb-3">
        <div class="card-header">
            <h5 class="card-title"><a class="link" href="/post/{{id}}">{{post.title}}</a></h5>
            <div class="author">Posted by {{user.username}} on {{format_date post.date_created}}</div>
        </div>
        <div class="card-body">
            <p class="card-text">{{post.content}}</p>
        </div>
</div>